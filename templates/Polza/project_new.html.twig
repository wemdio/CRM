{% extends 'base.html.twig' %}

{% block title %}Создание нового проекта{% endblock %}

{% block body %}
<style>
    .page-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 20px;
    }
    
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--primary-color);
        text-decoration: none;
        font-weight: 500;
        margin-bottom: 2rem;
        padding: 0.5rem 1rem;
        border-radius: 6px;
        transition: all 0.3s ease;
    }
    
    .back-link:hover {
        background: #f8f9fa;
        text-decoration: none;
        color: var(--secondary-color);
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 2rem;
    }
    
    .page-title {
        color: var(--text-dark);
        font-size: 2.5rem;
        font-weight: 700;
        margin: 0 0 1rem 0;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    
    .page-subtitle {
        color: var(--text-light);
        font-size: 1.1rem;
        margin: 0;
    }
    
    .info-note {
        background: #e3f2fd;
        padding: 1.5rem;
        border-radius: 8px;
        margin-bottom: 2rem;
        border-left: 4px solid var(--primary-color);
        display: flex;
        align-items: flex-start;
        gap: 1rem;
    }
    
    .info-icon {
        font-size: 1.5rem;
        flex-shrink: 0;
    }
    
    .info-content {
        flex: 1;
    }
    
    .info-title {
        font-weight: 600;
        color: var(--text-dark);
        margin-bottom: 0.5rem;
    }
    
    .info-text {
        color: var(--text-light);
        margin: 0;
        line-height: 1.5;
    }
    
    /* Project Grid */
    .project-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
        margin-bottom: 2rem;
    }
    
    .project-card {
        background: var(--bg-white);
        border-radius: 12px;
        padding: 1.5rem;
        box-shadow: var(--shadow);
        border: 1px solid var(--border-color);
    }
    
    .project-card.full-width {
        grid-column: 1 / -1;
    }
    
    .card-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 2px solid var(--border-color);
    }
    
    .card-icon {
        font-size: 1.5rem;
    }
    
    .card-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--text-dark);
        margin: 0;
    }
    
    /* Form Styles */
    .form-grid {
        display: grid;
        gap: 1.5rem;
    }
    
    .form-group {
        margin-bottom: 1.5rem;
    }
    
    .form-label {
        display: block;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-light);
        margin-bottom: 0.5rem;
    }
    
    .required-field::after {
        content: " *";
        color: #f44336;
    }
    
    .form-control {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid var(--border-color);
        border-radius: 6px;
        font-size: 0.95rem;
        transition: all 0.3s ease;
        background: white;
    }
    
    .form-control:focus {
        outline: none;
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
    }
    
    textarea.form-control {
        min-height: 100px;
        resize: vertical;
    }
    
    .form-control:invalid {
        border-color: #f44336;
    }
    
    .form-control:valid {
        border-color: #4CAF50;
    }
    
    /* Action Buttons */
    .action-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        margin-top: 2rem;
        padding-top: 2rem;
        border-top: 1px solid var(--border-color);
    }
    
    .btn-save {
        background: linear-gradient(135deg, #4CAF50, #45a049);
        color: white;
        border: none;
        padding: 0.75rem 2rem;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .btn-save:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-hover);
    }
    
    .btn-cancel {
        background: linear-gradient(135deg, #f44336, #d32f2f);
        color: white;
        border: none;
        padding: 0.75rem 2rem;
        border-radius: 8px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        text-decoration: none;
    }
    
    .btn-cancel:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-hover);
        text-decoration: none;
        color: white;
    }
    
    /* Responsive */
    @media (max-width: 1024px) {
        .project-grid {
            grid-template-columns: 1fr;
        }
    }
    
    @media (max-width: 768px) {
        .page-title {
            font-size: 2rem;
        }
        
        .action-buttons {
            flex-direction: column;
        }
        
        .btn-save,
        .btn-cancel {
            justify-content: center;
        }
    }
</style>

<div class="page-container">
    <a href="{{ path('app_projects') }}" class="back-link">
        ← Назад к списку проектов
    </a>

    <div class="page-header">
        <h1 class="page-title">Создание нового проекта</h1>
    </div>

    <div class="info-note">
        <div class="info-icon">💡</div>
        <div class="info-content">
            <div class="info-title">Информация</div>
            <p class="info-text">После создания проекта вы сможете редактировать все поля на странице проекта.</p>
        </div>
    </div>
    
    {{ form_start(form, {'attr': {'id': 'project-form', 'class': 'project-form'}}) }}
    
    <div class="project-grid">
        <!-- Главная информация -->
        <div class="project-card full-width">
            <div class="card-header">
                <div class="card-icon">📋</div>
                <h2 class="card-title">Главная информация</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.name.vars.id }}" class="form-label required-field">Название проекта</label>
                    {{ form_widget(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Введите название проекта', 'required': true}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.company.vars.id }}" class="form-label">Компания</label>
                    {{ form_widget(form.company, {'attr': {'class': 'form-control', 'placeholder': 'Введите название компании'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.hypothesis.vars.id }}" class="form-label">Гипотеза</label>
                    {{ form_widget(form.hypothesis, {'attr': {'class': 'form-control', 'rows': 4, 'placeholder': 'Опишите гипотезу проекта'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.brief.vars.id }}" class="form-label">Бриф</label>
                    {{ form_widget(form.brief, {'attr': {'class': 'form-control', 'placeholder': 'Введите ссылку на бриф'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.offer.vars.id }}" class="form-label">Оффер</label>
                    {{ form_widget(form.offer, {'attr': {'class': 'form-control', 'placeholder': 'Введите ссылку на оффер'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.site.vars.id }}" class="form-label">Сайт</label>
                    {{ form_widget(form.site, {'attr': {'class': 'form-control', 'placeholder': 'Введите URL сайта'}}) }}
                </div>
            </div>
        </div>

        <!-- Информация проекта -->
        <div class="project-card">
            <div class="card-header">
                <div class="card-icon">📊</div>
                <h2 class="card-title">Информация проекта</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.direction.vars.id }}" class="form-label">Направление</label>
                    {{ form_widget(form.direction, {'attr': {'class': 'form-control', 'placeholder': 'Введите направление проекта'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.status.vars.id }}" class="form-label required-field">Статус проекта</label>
                    {{ form_widget(form.status, {'attr': {'class': 'form-control', 'required': true}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.kpi.vars.id }}" class="form-label">KPI проекта</label>
                    {{ form_widget(form.kpi, {'attr': {'class': 'form-control', 'rows': 3, 'placeholder': 'Введите KPI проекта'}}) }}
                </div>
            </div>
        </div>

        <!-- Оборот средств -->
        <div class="project-card">
            <div class="card-header">
                <div class="card-icon">💰</div>
                <h2 class="card-title">Оборот средств</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.payment.vars.id }}" class="form-label">Способ оплаты</label>
                    {{ form_widget(form.payment, {'attr': {'class': 'form-control', 'placeholder': 'Введите способ оплаты'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.amount.vars.id }}" class="form-label">Сумма услуг</label>
                    {{ form_widget(form.amount, {'attr': {'class': 'form-control', 'placeholder': 'Введите сумму услуг'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.format.vars.id }}" class="form-label">Формат работы</label>
                    {{ form_widget(form.format, {'attr': {'class': 'form-control', 'placeholder': 'Введите формат работы'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.expenses.vars.id }}" class="form-label">Расходы за месяц</label>
                    {{ form_widget(form.expenses, {'attr': {'class': 'form-control', 'placeholder': 'Введите расходы за месяц'}}) }}
                </div>
            </div>
        </div>

        <!-- Даты -->
        <div class="project-card">
            <div class="card-header">
                <div class="card-icon">📅</div>
                <h2 class="card-title">Даты проекта</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.contract.vars.id }}" class="form-label">Дата договора</label>
                    {{ form_widget(form.contract, {'attr': {'class': 'form-control', 'placeholder': 'Введите дату договора'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.launch.vars.id }}" class="form-label">Дата запуска</label>
                    {{ form_widget(form.launch, {'attr': {'class': 'form-control', 'placeholder': 'Введите дату запуска'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.deadline.vars.id }}" class="form-label">Дедлайн</label>
                    {{ form_widget(form.deadline, {'attr': {'class': 'form-control', 'placeholder': 'Введите дедлайн'}}) }}
                </div>
            </div>
        </div>

        <!-- Состав -->
        <div class="project-card">
            <div class="card-header">
                <div class="card-icon">👥</div>
                <h2 class="card-title">Состав команды</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.specialist.vars.id }}" class="form-label">Специалист</label>
                    {{ form_widget(form.specialist, {'attr': {'class': 'form-control', 'placeholder': 'Введите специалиста'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.mentor.vars.id }}" class="form-label">Наставник</label>
                    {{ form_widget(form.mentor, {'attr': {'class': 'form-control', 'placeholder': 'Введите наставника'}}) }}
                </div>
            </div>
        </div>

        <!-- Дополнительная информация -->
        <div class="project-card full-width">
            <div class="card-header">
                <div class="card-icon">📝</div>
                <h2 class="card-title">Дополнительная информация</h2>
            </div>
            <div class="form-grid">
                <div class="form-group">
                    <label for="{{ form.description.vars.id }}" class="form-label required-field">Описание проекта</label>
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': 6, 'placeholder': 'Опишите проект', 'required': true}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.information.vars.id }}" class="form-label">Информация</label>
                    {{ form_widget(form.information, {'attr': {'class': 'form-control', 'rows': 4, 'placeholder': 'Введите дополнительную информацию'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.history.vars.id }}" class="form-label">История</label>
                    {{ form_widget(form.history, {'attr': {'class': 'form-control', 'rows': 4, 'placeholder': 'Введите историю проекта'}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.userPosition.vars.id }}" class="form-label required-field">Количество месяцев</label>
                    {{ form_widget(form.userPosition, {'attr': {'class': 'form-control', 'placeholder': 'Введите сроки работ', 'required': true}}) }}
                </div>
                
                <div class="form-group">
                    <label for="{{ form.turnover.vars.id }}" class="form-label">Оборот</label>
                    {{ form_widget(form.turnover, {'attr': {'class': 'form-control', 'placeholder': 'Введите оборот'}}) }}
                </div>
            </div>
        </div>
    </div>

    <!-- Кнопки действий -->
    <div class="action-buttons">
        <button type="submit" class="btn-save">
            <span>🚀</span>
            <span>Создать проект</span>
        </button>
        <a href="{{ path('app_projects') }}" class="btn-cancel">
            <span>❌</span>
            <span>Отмена</span>
        </a>
    </div>
    {{ form_end(form) }}
</div>

<script>
    // Автофокус на первое поле
    document.addEventListener('DOMContentLoaded', function() {
        const firstInput = document.querySelector('.form-control');
        if (firstInput) {
            firstInput.focus();
        }
    });

    // Валидация обязательных полей
    document.querySelectorAll('.required-field').forEach(label => {
        const inputId = label.getAttribute('for');
        const input = document.getElementById(inputId);
        if (input) {
            input.addEventListener('blur', function() {
                if (!this.value.trim()) {
                    this.style.borderColor = '#f44336';
                } else {
                    this.style.borderColor = '#4CAF50';
                }
            });
        }
    });
</script>
{% endblock %}